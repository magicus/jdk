package build.tools.configure.checks;

public class CheckPaths {
    /*
    check that all paths make sense
    check that we are not creating config in the wrong dir
    check that dirs are on local disk

    check that read permissions seem fine

     */
}
