package build.tools.configure.nativelibs;

public class LibFreetype {
    /* freetype can be compiled as "bundled" or "system". for "bundled", this means
    using the bundled source code and bundling the lib with the product.
    for "system", this means compiling using a system-installed libfreetype, and also
    having the runtime require a system-installed libfreetype.
     */
}
