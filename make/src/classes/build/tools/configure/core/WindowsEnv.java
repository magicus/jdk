package build.tools.configure.core;

public class WindowsEnv {
    /*
    determine env type
    handle paths
    determine win root and win tmp
    windows version, and winenv version (wsl/cygwin/msys version???)
    emulated linux version (if applicable)
     */
}
